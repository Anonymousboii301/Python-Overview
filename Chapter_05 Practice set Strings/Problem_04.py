# 4. Replace the double space from problem 3 with single spaces.

name = "Ayush is a  good boi."
print(name)

print(name.find("  "))

print(name.replace("  " , " "))

print(name)  
# Strings are immutable which means when we use these function our string remains unchanged